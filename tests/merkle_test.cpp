#include <gtest/gtest.h>
#include "Block.h"

TEST(MerkleHashes, MerkleProof) {
    // ensure correct merkle proof is returned
    // i should create a block with some transactions and then ensure the transactions are verifeid
    // mock data that i need for the hashes is just the transactions and the proofs
}
